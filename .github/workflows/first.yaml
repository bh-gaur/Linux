name: Test variable

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
  #   paths:
  #     - 'aws-cloud/ec2/*'
  # pull_request:
  #   branches:
  #     - 'main'

# env:
#   DAY_OF_WEEK: Monday

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello Mona it's Monday"
        if: ${{ env.MY_COMMAND == 'ls' }}
        run: echo "$Greeting $First_Name. Today is $DAY_OF_WEEK!"
        env:
          First_Name: Mona

